module COMBAT {
    /**
     * Definition of a hitbox
     * @author Anthony
     */
    export interface HitBox {
        width: number,
        height: number,
        wOffset?: number,
        hOffset?: number
    }
}